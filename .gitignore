# Audio lives on Hugging Face (dataset arjun10g/papers-audio), never in git.
# tools/publish.py uploads audio/ and writes library.json; the app streams from HF.
audio/
*.mp3
*.wav
_concat.txt

# Files from the pre-September-2026 flat layout, parked for reference.
_legacy/

# The old per-lecture Speechify scripts carried a hardcoded API key.
*_speech.py

# Editor / OS / Python noise
.DS_Store
.claude/
*.bak
__pycache__/
.venv/
